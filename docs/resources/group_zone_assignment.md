---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "poweradmin_group_zone_assignment Resource - poweradmin"
subcategory: ""
description: |-
  Assigns a DNS zone to a Poweradmin group. All members of the group gain access to the zone. Requires Poweradmin 4.2.0+.
---

# poweradmin_group_zone_assignment (Resource)

Assigns a DNS zone to a Poweradmin group. All members of the group gain access to the zone. Requires Poweradmin 4.2.0+.

## Example Usage

```terraform
# Assign a zone to a group — all group members gain access to the zone
resource "poweradmin_group" "ops" {
  name        = "operations"
  description = "Operations team"
}

resource "poweradmin_zone" "example" {
  name = "example.com"
  type = "MASTER"
}

resource "poweradmin_group_zone_assignment" "ops_example" {
  group_id = poweradmin_group.ops.id
  zone_id  = poweradmin_zone.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `group_id` (Number) ID of the group
- `zone_id` (Number) ID of the zone to assign to the group

### Read-Only

- `id` (String) Composite identifier in the format `group_id/zone_id`

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import a group zone assignment by group_id/zone_id
terraform import poweradmin_group_zone_assignment.ops_example 1/10
```
